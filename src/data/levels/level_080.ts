import { buildLevel } from './utils';
import { IMAGE_TEMPLATES } from '../imageTemplates';
import { PALETTES } from '../palettes';

// Level 80: Pirate Face — hard
const LEVEL_080 = buildLevel(80, IMAGE_TEMPLATES[73], PALETTES[3], 'hard');
export default LEVEL_080;
