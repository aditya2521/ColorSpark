import { buildLevel } from './utils';
import { IMAGE_TEMPLATES } from '../imageTemplates';
import { PALETTES } from '../palettes';

// Level 60: Little Mermaid — hard
const LEVEL_060 = buildLevel(60, IMAGE_TEMPLATES[33], PALETTES[22], 'hard');
export default LEVEL_060;
