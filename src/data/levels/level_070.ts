import { buildLevel } from './utils';
import { IMAGE_TEMPLATES } from '../imageTemplates';
import { PALETTES } from '../palettes';

// Level 70: Mermaid Face — hard
const LEVEL_070 = buildLevel(70, IMAGE_TEMPLATES[63], PALETTES[22], 'hard');
export default LEVEL_070;
