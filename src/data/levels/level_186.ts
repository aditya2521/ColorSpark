import { buildLevel } from './utils';
import { IMAGE_TEMPLATES } from '../imageTemplates';
import { PALETTES } from '../palettes';

// Level 186: Hydra — expert
const LEVEL_186 = buildLevel(186, IMAGE_TEMPLATES[185], PALETTES[21], 'expert');
export default LEVEL_186;
